# Security

`vscode-debug-preflight` parses local JSONC configuration and checks references. It does not execute configured commands, load VS Code extensions, use credentials, make network requests, or write to the checked workspace.

Report security issues privately to the repository maintainer rather than including credentials or private workspace data in a public issue.
